package com.example.voicedrop.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Brand Palette ────────────────────────────────────────────────────────────
val DeepSpace       = Color(0xFF0B0120)  // Darkest background
val SpaceViolet     = Color(0xFF130830)  // Surface
val SpaceVioletAlt  = Color(0xFF1C0F45)  // Surface variant / elevated
val ElectricViolet  = Color(0xFF7C3AED)  // Primary
val LightViolet     = Color(0xFFAB74FF)  // Primary container / secondary
val SoftIndigo      = Color(0xFF4F46E5)  // Secondary
val NeonViolet      = Color(0xFFBE8BFF)  // On-primary container text

// ─── Status Colors ────────────────────────────────────────────────────────────
val StatusSent      = Color(0xFF22C55E)  // Green — success
val StatusPending   = Color(0xFFF59E0B)  // Amber — in progress
val StatusFailed    = Color(0xFFEF4444)  // Red — error

// ─── Record Button ────────────────────────────────────────────────────────────
val RecordRed       = Color(0xFFFF3B3B)
val RecordRedDim    = Color(0xFF7F1D1D)

// ─── Neutral ──────────────────────────────────────────────────────────────────
val OnSurface       = Color(0xFFF1EEFF)
val OnSurfaceVar    = Color(0xFFB8ADE0)
val Outline         = Color(0xFF3D2D6E)
val OutlineDim      = Color(0xFF261B4F)